// services/profile.js
const updateUserProfile = async (userId, updatedUser) => {
    // Implement the logic for updating the user profile, including saving the photoUrl
    // For example:
    // const photoUrl = `http://localhost:${process.env.PORT}/uploads/${updatedUser.photoUrl}`;
    // await User.findByIdAndUpdate(userId, { photoUrl, ...otherFields });
  };
  
  module.exports = { updateUserProfile };
  